# Production asset wave one

**Status:** exact-head release candidate for the RODOH 1.0 production dignity floor.

This wave adds specific, runtime-loaded assets to three shipped surfaces. It does not introduce an asset generator, theme framework, or generalized art abstraction. Each file exists because a named cartridge or system surface still required a finished authored environment.

## The Waking Tower

The Waking Tower already had cartridge-owned pixel roles, encounter motifs, palette, and a distinct material language. Its remaining visible gap was the principal inhabited Hall. This wave adds:

- `waking-tower-environment.svg`, a violet-night survey hall with an orrery window, spiral galleries, raid table, banners, and blue survey lanterns;
- `seren-vale-portrait.svg`, a full lead portrait for Seren Vale, Warden of the Lamplit Survey;
- `waking-tower-foreground.svg`, a noninteractive framing layer of tower arches, chains, balcony rails, and lantern glass.

The assets bind only while `data-cartridge="karazhan"` is active. They do not alter the historical compatibility id, challenge law, people, or run custody.

## The Kind Gods of Ilyon

Ilyon already had a complete cartridge-owned pixel cast, Godscar motifs, evidence presentation, and oceanic civic-observatory material language. This wave adds the full-frame environment and lead evidence portrait needed to bring its principal civic space to the same production floor:

- `ilyon-observatory-environment.svg`, the Confluence Observatory built from nacre, coral, brass, tideglass, astronomy, and open water;
- `aster-neral-portrait.svg`, a full lead portrait for Aster Neral, the astronomer who identifies Benefactor geometry in the dead neighboring star;
- `ilyon-observatory-foreground.svg`, a noninteractive framing layer of nacre arches, coral, tideglass rails, and brass instruments.

The first Godscar cast receipt is enlarged because Aster is already the first authored cast member. The receiver does not invent a lead or reorder the source.

## Rodoh cartridge bay

The player shell previously presented a complete functional shelf against a neutral gradient. That was sufficient for architecture and custody acceptance, but not for the finished local-first product surface. This wave adds:

- `rodoh-bay-environment.svg`, a dark cartridge vault with physical cases, offline indicators, brass identity framing, and a central loading dais;
- `rodoh-bay-foreground.svg`, a fixed decorative vault frame that remains behind every cartridge control.

The bay environment belongs to Rodoh only. It disappears when a holder enters a cartridge, and it never supplies the identity of an imported world.

## Accessibility and custody

Every new SVG has semantic title and description metadata, an explicit view box, no embedded raster or remote dependency, and a project-owned provenance manifest. CSS removes decorative images in forced-colors mode, preserves reduced-motion behavior, and keeps pointer events away from foreground art. The mobile bay is scrollable, keeps explicit touch-target floors, and places focus indicators above the framing layer.

The browser acceptance checks the actual Vite-loaded asset URLs on desktop and mobile projects through the existing Playwright matrix. Static contracts verify semantic SVG metadata, exact provenance, cartridge scoping, system hooks, mobile fallbacks, and forced-colors behavior.

The dedicated application run completed TypeScript, the focused asset contracts, the complete World regression suite, production build, Chromium installation, desktop and mobile browser acceptance, and publication of the exact `Player.tsx` binding. The temporary application workflow and binder were then removed before final PR-head qualification.

## Release boundary

This wave closes three named visible gaps. It does not claim that every challenge, item, exterior location, or future cartridge already has a unique full-frame illustration. Further production waves remain ordinary content work, not prerequisites hidden behind a generalized asset system.