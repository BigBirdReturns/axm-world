# Burn Protocol manifest-derived corpus atlas

## Classification

This stacked transaction turns the admitted external corpus index into a read-only atlas inside the continuing Rodoh world. It does not add corpus metadata or image bytes to Arc law, organization state, reports, saves, portable-run extensions, static build output, service-worker storage, or canonical Burn history.

The base is the qualified live evidence projection:

```text
probe/burn-protocol-live-evidence-projection-v1
1b1dbf0bf8036bcd37eab8b22a6eb53cd6d777df
```

The publication identity remains:

```text
burn-protocol-disclosure-probe
cart1_870f3dfcab909fc9aace115e2c46cd30268339f80bc87a14f0eebcc4e2c28c3e
```

## Actors and authority

Arc controls authored law, cartridge identity, challenge outcomes, and the read-only and counterfactual canon boundary. The activation overlay, activation receipt, and manifest-derived asset index control external custody standing. The holder controls local asset selection. The qualified receiver controls byte length, SHA-256 verification, and creation of revocable object URLs.

The atlas receives only two process-local records:

```text
admitted manifest metadata
verified external asset session
```

It receives no `ArcWorld`, `ArcInteraction`, challenge, report, resolution, save, export, or state-effect callback. It therefore cannot affect a watch, satisfy an access gate, create a consequence, or modify the continuing run.

## Catalog admission

The existing receiver remains the visible authority for custody admission. A bounded capture wrapper observes only the exact three-file custody input and independently applies the same format, size, identity, canon-boundary, activation-receipt, overlay, and index checks through `prepareExternalAssetCustody`.

A passing catalog is held in module process memory under the exact authored Arc digest. It contains copied manifest rows, classification counts, byte totals, custody hashes, and evidence standing. It contains no selected `File`, `Blob`, `ArrayBuffer`, or object URL.

A changed or incomplete custody selection clears the prior catalog. A cold page reload removes the catalog automatically because it is never persisted.

## Coordinate law

The atlas claims episode, chapter, and ordinal coordinates only when a verified manifest path contains explicit coordinate tokens such as:

```text
E12-C3-P01
E12-C3-PLATE-01
EP13/CH1/PANEL-01
```

No coordinate is inferred from manifest array order, classification, filename proximity, asset count, or story knowledge. A record without an explicit episode and chapter pair remains **unlocated evidence**. This preserves the source ledger without converting a presentation guess into corpus fact.

The normalized atlas sorts:

```text
episode number
→ chapter number
→ panel, plate, reader evidence, visual evidence
→ explicit ordinal
→ Unicode codepoint path order
```

## Verified and manifest-only states

Every indexed record appears as metadata. Only a row also present in the qualified external asset session is marked verified. Only a verified PNG, JPEG, or WebP with a receiver-created object URL can be previewed.

The atlas therefore distinguishes:

```text
manifest only     path, classification, bytes, SHA-256, explicit coordinates
verified byte     the same metadata plus a process-local preview capability
```

Selecting an episode or chapter, opening a preview, or changing atlas view changes presentation state only. Unselected indexed assets never become browser image sources.

## Fixture qualification

The dedicated workflow builds a six-record fixture spanning:

```text
Episode 12, Chapter 2
Episode 12, Chapter 3
Episode 13, Chapter 1
one scroll plate
one unlocated visual-evidence record
```

Only one panel is selected for verification. The qualification must prove that the atlas shows six indexed records, one verified record, two explicit episodes, three explicit chapters, and one unlocated record while decoding only the selected one-pixel raster.

The workflow also requires:

```text
strict TypeScript
focused custody and atlas tests
ordinary World production build
no fixture raster bytes in static output
no fixture asset path in static output
desktop and mobile atlas journeys
before and after durable run equality
no atlas material in portable export
cold reload preserves the run and forgets the atlas
no external request
no horizontal overflow
```

## Evidence ledger

The evidence tier is mechanism qualification; the venue is the stacked corpus-atlas branch; the target is the complete production asset index derived from the exact private A13C1 handoff; the upside is corpus-scale navigation without bundling or authoring drift; the downside is that verified previews must be reselected after every cold process boundary; the failure mode is guessed coordinates, unverified preview, persistence, export leakage, static bundling, or metadata acquiring authority over authored law.

The control question is whether the holder can inspect the shape and coverage of the Burn corpus while every unverified byte remains unavailable and every inferred relationship remains visibly noncanonical.
