# Burn Protocol corpus receiver probe

## Classification

This transaction is an unbundled, metadata-only Arc-to-World receiver probe. It does not add *The Burn Protocol* to Rodoh's bundled cartridge library, assign it a Program of Record, register a new source plane, import panel or plate imagery, alter the sealed narrative estate, or claim custody of files that were absent from the supplied record.

The Arc authority is the exact `axm-arc` publication head:

```text
4b076089f9b7ae1949ba8fac45f2373aeeb5b344
```

That authority publishes `The Burn Protocol: Disclosure and Repair` as a `common-ship-pocket/1` source and Engine 1.3 Arc. Its inherited parent claim remains the supplied v0.58.0 SHA-256:

```text
b3b299e14d8c22cde88629eb6bc4d197b8f8015eec7bf46b95f0de2a31b5f0df
```

The calibrated cartridge content identity expected by this receiver is:

```text
cart1_870f3dfcab909fc9aace115e2c46cd30268339f80bc87a14f0eebcc4e2c28c3e
```

## Live-resolver calibration

The first exact browser transaction correctly imported and entered the original publication, but its initial hearing resolved `partial` on both desktop and mobile even though the static preview classified the party as reliable. The preview used the deterministic aggregate contribution of 56.8 against a threshold of 50. The historical Engine 1.3 resolver then performed one aggregate roll for each assigned actor, and one of the six accepted rolls landed at 48.24. Because access law requires a success record, the partial hearing could not open the next watch.

Arc now carries that finding as publication law rather than hiding it in the receiver test. Version 0.1.1 calibrates the four check thresholds to:

```text
open-the-six-repository-hearing           45
assign-the-six-withdrawal-mandates        50
repair-the-first-public-corridor          30
publish-the-read-only-reconstruction      40
```

The calibration changes no actor, role, embodiment profile, composition constraint, decision horizon, evidence boundary, outcome text, state effect, custody fact, or canon relation. Its dedicated Arc gate uses default founding, all six named founders, one authored Watch token per operation, ordinary `runCycle`, and requires four ordered accepted successes. The World receiver independently recomputes the new cartridge digest and verifies the same source thresholds before browser play.

## Receiver transaction

The dedicated workflow performs the actual portable-artifact path rather than importing a locally rewritten World copy:

```text
check out exact World candidate
  → check out exact Arc publication authority
  → compile source, Arc, corpus receipt, and publication receipt in Arc
  → verify every published byte against the Arc receipt
  → validate the Arc through World's vendored Engine 1.3 seam
  → import it through the visible Rodoh cartridge bay
  → enter the neutral World receiver
  → complete all four governance watches
  → export axm-cartridge-run/v3
  → clear browser custody
  → import the exact run
  → resume the four-recorded-watch world
```

The four authored watches are:

```text
open-the-six-repository-hearing
assign-the-six-withdrawal-mandates
repair-the-first-public-corridor
publish-the-read-only-reconstruction
```

Each watch must admit the same six named founders and pass role coverage, temporal overlap, habitat compatibility, translation resilience, handoff continuity, and life-fraction fairness through Arc-owned composition law. The browser journey then requires a successful accepted result for every watch and verifies the four persistent consequence receipts in the exported run.

## Player-facing boundary

The imported object must remain `imported-unsigned`, carry no Program of Record, and render in Rodoh's neutral theme. The run may use Board, Map, Hall, and report surfaces, but the receiver must not dress the cartridge in another bundled world's motifs or claim that absent Burn visual payloads were loaded.

The browser test retains desktop and mobile evidence for the cartridge bay, initial live world, four-watch completion state, and resumed world. It also records every external request and requires the list to remain empty.

## Canon boundary

The source extension states:

```text
inherited history  read-only
live runs           counterfactual-only
story changes       none
panel payloads      not-present
```

A passing receiver journey therefore proves that the published governance model can become a deterministic, resumable live Rodoh world. It does not make any generated outcome canonical and does not independently validate the unavailable estate ZIP, fresh-session handoff ZIP, image payloads, manifests, audits, or complete A13C1 production-contract bytes.

## Next acceptance boundary

After this receiver path is green, the next payload-backed transaction is exact handoff intake. It must verify the sealed estate and handoff archives before replacing the metadata-only corpus receipt, then add an asset index and a dedicated Burn presentation pack without changing Arc's authored law or the read-only/counterfactual boundary.
