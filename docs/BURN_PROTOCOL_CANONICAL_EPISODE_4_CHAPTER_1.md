# The Burn Protocol through Episode 4, Chapter 1

## Classification

This transaction receives the continuing `burn-protocol` Arc through Episode 4, Chapter 1. It extends the accepted fixed story from `E03-C3-P60` to `E04-C1-P01` and exposes twenty additional canonical panel positions through the existing `SequenceHost`.

No production reader, session, asset-verification, canonical-story runtime, or capability-dispatch file changes in this transaction. The larger Arc is the only new player-facing input.

## Authored extent

```text
Episode 4: Fractured Allegiances
Chapter 1: Osyraa's Offer

E04-C1-P01 through E04-C1-P20

prior canonical panel
E03-C3-P60

next unpublished panel
E04-C2-P21
```

The combined cartridge contains:

```text
4 episodes
10 chapters
200 panel positions
40 plate assets
0 choices
0 challenges
0 roles
```

## Runtime authority

World dispatches the validated Arc through the already accepted capability boundary:

```text
axm.canonical-story@1
  → SequenceHost

otherwise
  → simulation runtime
```

The Episode 4 Arc must not found an organization or expose roster, assignment, resource, challenge, success, partial, failure, or choice interfaces.

The existing reading session remains:

```text
rodoh-canonical-story-session/1
```

It stores only the exact Arc digest and validated canonical cursor. The holder's media bytes remain process-local and must be selected again after a reload.

## Episode seam

The reader derives the series seam directly from Arc data:

```text
E03-C3-P60
  → Next
  → E04-C1-P01
```

The reverse transition is equally authoritative:

```text
E04-C1-P01
  → Previous
  → E03-C3-P60
```

No Episode 4-specific navigation code is allowed. Selecting the A04C1 chapter control opens its explicit opening panel. Advancing from `E04-C1-P20` leaves the cursor at P20 and emits the declared outside continuation `E04-C2-P21`.

## Source and asset boundary

The Arc carries exact manifest metadata for twenty A04C1 panel assets and four A04C1 plate assets. The exact Episode 4 source, compiled reader source, A04C1 lettering source, and plate-composition map are not present in World.

The reader therefore displays:

```text
Canonical text source required
```

for every A04C1 panel and keeps plate mode blocked. It does not derive dialogue, captions, sound effects, alt text, or plate ranges from parity, causal, recovery, filename, or asset-order evidence.

The existing holder-controlled asset verifier remains unchanged. A selected file must match one canonical path, exact byte count, and exact SHA-256 before World creates a temporary object URL. No media byte enters local storage, portable runs, static output, or simulation state.

The inherited source-required asset at `E03-C2-P31` remains the only missing exact media digest. Episode 4 neither removes that record nor gives it a fabricated hash.

## Qualification path

Desktop and mobile must each prove:

```text
visible 200-panel cartridge import
P01 opening under the exact Arc digest
10 chapter controls
E03-C3-P41 through E03-C3-P60
E03-C3-P60 → E04-C1-P01
20 A04C1 panel controls
4 A04C1 plate assets
canonical text refusal
P01 → P10
reload and exact P10 resume
P10 → P20
P20 → E04-C2-P21 extent completion
P20 → P19 reverse traversal
E04-C1-P01 → E03-C3-P60 reverse episode seam
no simulation surfaces
no external requests
no horizontal overflow
```

All previously accepted Burn compatibility journeys also rerun against the unchanged reader.

## Evidence ledger

The evidence tier is source-ledger receiver implementation; the venue is the Episode 4 Chapter 1 World branch and its pinned Arc authority; the target is production-complete A04C1 after exact source-byte intake; the upside is proof that another episode begins through data alone; the downside is that canonical expression and plate composition remain blocked; the failure mode is hidden reader drift, cursor loss at the series seam, static content bundling, inferred expression, or fabricated asset custody.

The control question is whether Chapter 2 can be appended to the same Arc and appear automatically under the existing Episode 4 chapter index.
