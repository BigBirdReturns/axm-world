# Dist harvest

The uploaded `AXM.zip` contained two standalone HTML exports:

- `AXM-ARC.standalone.html`
- `AXM-WORLD.standalone.html`

This folder contains sanitized templates and an inventory only. Raw font files and original embedded manifests are intentionally excluded.

Use this only as QA/demo reference. Make source changes in the repo, not in standalone HTML.
